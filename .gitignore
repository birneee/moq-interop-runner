# CA certificate for corporate proxy (copied during build)
extra-ca.pem

# Generated certificates
certs/

# Test results
results/

# mlog output
mlog/

# Builds framework - cloned source repos and build artifacts
builds/*/.sources/
builds/*/.last-build.json

# nix build output symlinks (e.g. builds/birneee-moq/build.sh's `nix build -o`)
builds/*/.result-*

# Extra CA certificates for TLS inspection (environment-specific)
extra-ca.pem
builds/*/extra-ca.pem

# Rust build artifacts
builds/moq-dev-rs/target/

# Node/Bun dependencies
builds/moq-dev-js/node_modules/

# Local planning notes
.plan/
